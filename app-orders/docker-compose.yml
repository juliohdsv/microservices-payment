services:
  pg:
    image: bitnami/postgresql:latest
    ports:
      - "5482:5432"
    environment:
      - POSTGRES_USERNAME=docker
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=orders
    volumes:
      - ./docker:/docker-entry-point-initdb.d
